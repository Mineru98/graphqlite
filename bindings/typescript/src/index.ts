// GraphQLite TypeScript bindings — public entry point.
//
// This is the F-01 scaffolding skeleton. The public surface (`graph()`,
// `connect()`, `graphs()`) lands in later elements (#2~#6 and beyond); see
// docs/internal/typescript-bindings-design.md §6.
//
// Intentionally an empty export so `tsc --noEmit` and `node --test` pass on the
// bare skeleton without pulling in the extension loader yet.
export {};
