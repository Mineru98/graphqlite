# before — nodeSimilarity topK 무시 분기 (수정 전)

## TS similarity.ts
72:  } else if (threshold > 0 && topK > 0) {
## Python similarity.py
35:        elif threshold > 0 and top_k > 0:
## Rust similarity.rs
35:            _ if threshold > 0.0 && top_k > 0 => {
